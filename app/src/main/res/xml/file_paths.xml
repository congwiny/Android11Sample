<?xml version="1.0" encoding="utf-8"?>
<paths>

    <!--
    files-path
    Context.getFilesDir()

    cache-path
    Context.getCacheDir()

    external-path
    Environment.getExternalStorageDirectory()

    external-files-path
    Context.getExternalFilesDir(null)

    external-cache-path
    Context.getExternalCacheDir()

    external-media-path
    Context.getExternalMediaDirs()

    root-path
    /
    -->
    <root-path name="root" path="." />
    <files-path name="files" path="share-files" />
    <cache-path name="cache" path="share-cache" />
    <external-path name="11sample_external" path="." />
    <external-files-path name="external-files" path="ext-files" />
    <external-cache-path name="external-cache" path="ext-cache" />
</paths>